# Known Issues


# v0.4.0:0

- An instance of undefined behavior (UB) occurs between lines 88 and 99 in `com.qaos.sysui/Dev/Main.cpp`, specifically between the `Log("Draw");` and `Log("Graphic.Flush");` calls.
